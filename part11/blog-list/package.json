{
  "name": "blog-list",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client-install": "cd frontend && npm install",
    "server-install": "cd backend && npm install",
    "client-build": "npm run client-install && npm run server-install && cd frontend && npm run build",
    "build": "npm run client-build",
    "start": "concurrently \"cd frontend && npm start\" \"cd backend && npm run dev\"",
    "start-prod": "npm run build && cd backend && npm start"
  },
  "author": "elias hietanen",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
